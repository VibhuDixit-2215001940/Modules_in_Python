#uniform()--> It return any floating point no. between two no.
#-->It include lower limit but exclude upper limit
from random import *
n=uniform(5,10)     #Can directly access the f() in module
print(n)
